import { StyleSheet } from "react-native";

export const styles = new StyleSheet.create({
  content: {
    paddingBottom: 100,
  },
  noGroups: {
    color: "#fff",
    textAlign: "center",
    marginTop: 20,
    opacity: 0.4,
    fontSize: 16,
    marginHorizontal: 40,
  },
});
