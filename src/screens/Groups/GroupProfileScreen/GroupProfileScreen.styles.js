import { StyleSheet } from "react-native";

export const styles = new StyleSheet.create({
  content: {
    paddingHorizontal: 20,
    paddingTop: 20,
  },
  actionsContent: {
    marginBottom: 70,
    marginTop: 30,
  },
});
